# سجل التغييرات - StoryBot v2.3 REBORN (Manus Fixes)

تم تنفيذ الإصلاحات والتحسينات التالية بناءً على طلب المستخدم:

## [M-01] تحسين حماية السبام (Throttling)
- استثناء الأدمن (SUPER_ADMIN, OPERATOR, SUPPORT) من قيود الحماية.
- تقليل مهلة الانتظار الافتراضية إلى 0.5 ثانية بدلاً من 2 ثانية.

## [M-02] إصلاح زر اللغة
- تصحيح `callback_data` لزر اللغة في `utils/keyboards.py`.
- تعديل `handlers/language.py` لاستخدام تنسيق HTML وعرض القائمة الرئيسية فور تغيير اللغة.

## [M-03 & M-04] تحسين واجهة المستخدم
- إضافة معالجات لأزرار "حسابي"، "طلباتي"، "الدعم"، و"اللغة" في القائمة الرئيسية.
- توحيد استخدام تنسيق HTML في جميع الرسائل لضمان الموثوقية.

## [M-05] حقل معرف اللاعب الاختياري
- إضافة حقل `requires_player_id` في جدول المنتجات.
- تعديل واجهة الإضافة للأدمن لسؤال ما إذا كان المنتج يتطلب معرف لاعب أم لا.
- تعديل `order_service` للتحقق من المعرف فقط إذا كان مطلوباً.

## [M-06] تحسين نظام الكوبونات
- إضافة حماية (Try/Except) حول عملية التحقق من الكوبون لضمان عدم تعليق الطلب.
- إظهار رسالة خطأ واضحة للمستخدم في حال كان الكوبون غير صالح.

## [M-07] عرض الكوبونات للأدمن
- إضافة زر "📋 قائمة الكوبونات" في لوحة تحكم الكوبونات لعرض جميع الكوبونات النشطة وتفاصيلها.

## [M-08] إصلاح إضافة طرق الدفع
- تصحيح `callback_data` لزر إضافة طريقة دفع (`apm_add`).

## [M-09] تحسين عرض المستخدمين
- تحسين عرض قائمة المستخدمين في لوحة التحكم لتجنب الأخطاء عند وجود قيم `None`.
- تحسين عرض تفاصيل المستخدم.

## [M-10] تحسين الإحصائيات
- تحسين واجهة الإحصائيات واستخدام تنسيق HTML والخط العريض (Bold) للقيم المالية.

## [M-11] تحسين السجل والبث
- تحسين واجهة سجل العمليات (Audit Logs) ونظام البث الجماعي (Broadcast).

## تحسينات عامة
- ترتيب الـ Routers في `main.py` لضمان معالجة الأوامر بشكل صحيح (الأدمن أولاً).
- توحيد `parse_mode` ليكون HTML بشكل افتراضي في `main.py` وحذف التكرارات في المعالجات.
- إضافة الوظائف المفقودة في `db_manager` (مثل `get_all_coupons`).
- تحسين حماية البيانات (NULL handling) في جميع التقارير.
